const BasePage = require('./BasePage');

class AuthPage extends BasePage {
    constructor(driver) {
        super(driver);
    }

    // specific locators for auth could go here if using flutter semantic nodes
}

module.exports = AuthPage;
